懒加载和预加载

懒加载：对于需要加载很多静态资源的页面，为了提高页面性能。
可以在用户浏览到当前资源的时候在对资源进行请求和加载，是优化网页的一种方式
图片距离顶部的距离大于可视区域和滚动区域之和时加载
img[i].offsetTop < seeHeight + scrollTop
预加载：提前加载图片，当用户需要查看时可直接从本地缓存中渲染
预加载会增加服务器的压力，可以说是牺牲服务器前端性能，
换取更好的用户体验，这样可以使用户的操作得到最快的反映。
